<%- include('includes/header'); -%>
<%- include('includes/nav'); -%>

<div class="container">
        <div class="row">
            <div class="col">
                <hr> 
        <h1> Edit </h1>
        <style> h1 {color:darkorange; font-size:30px} </style>
  <form method="post" enctype="multipart/form-data">
    <div class="form-group">
      <label for="inputTitle"class="font-weight-normal">Category</label>
      <select class="custom-select col-sm-4" name="category">
        <option>Select category</option>
        <% for (var i=0; i < category.length; i++) { %>
          <option value="<%= category[i].id %>" <% if (feed.category_id == category[i].id) { %>selected<% } %>><%= category[i].title %></option>
        <% } %>
      </select>
    </div>

    <div class="form-group">
      <label for="inputTitle" class="font-weight-normal">Title</label>
      <input type="text" name="title" class="form-control" id="inputTitle" aria-describedby="inputTitle" placeholder="Enter title" value="<%= feed.title %>">
      <small id="titleHelp" class="form-text text-muted">피드의 제목을 입력해주세요.</small>
    </div>
    <div class="form-group">
      <label for="inputDesc"class="font-weight-normal">Description</label>
      <textarea name="desc" rows="5" cols="50" class="form-control" id="inputDesc"><%= feed.desc %></textarea>
      <small id="descHelp" class="form-text text-muted">내용을 입력해주세요.</small>
    </div>
    <div class="form-group">
      <label for="inputTitle"class="font-weight-normal">Teacher</label>
      <input type="text" name="author" class="form-control" id="inputAuthor" aria-describedby="inputAuthor" placeholder="Enter author" value="<%= feed.author %>">
      <small id="authorHelp" class="form-text text-muted">선생님의 이름을 입력해주세요. </small>
    </div>
    <div class="form-group col-sm-4">
        <div class="custom-file">
          <input type="file" name="upload" class="custom-file-input" id="inputGroupFile04" aria-describedby="inputGroupFileAddon04">
          <label class="custom-file-label" for="inputGroupFile04">Choose file</label>
          <small id="uploadHelp" class="form-text text-muted">
        교체할 사진을 업로드 해주세요.
        <% if (feed.upload) { %>현재 업로드된 파일: <%= feed.upload %><% } %>
      </small>
        </div>
    </div>
    <button type="submit" class="btn btn-warning">Complete</button>
    <a href="/icheck" class="btn btn-secondary">Cancel</a>
  </form>
  <br>
</div>
<%- include('includes/footer'); -%>


